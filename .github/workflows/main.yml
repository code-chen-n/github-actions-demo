name: CI
on: [push]
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - run: |
      echo $PATH
      node -v
      npm -v
      python --version
      java --version
      go version
      mvn -version
      docker version
      docker-compose -v
      git version
